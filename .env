# Server configuration
PORT=5000

# MySQL database configuration
DB_HOST=localhost      # Database host (use 127.0.0.1 if localhost causes issues)
DB_USER=root           # MySQL username
DB_PASSWORD=           # Set a strong password for production use
DB_NAME=hotel_management

# JWT secret for token generation 
JWT_SECRET=b9f8e5a9d9b0f2e4b1e1f8b2e9e0b1f5 # Ensure this is kept secret and never shared
   
# Email service credentials (use App Passwords for Gmail)
EMAIL_USER=mabuzaphila2@gmail.com
EMAIL_PASS=xhffgbplbkdmnkzh


# Optional: Add additional configurations if needed
# Example:
# LOG_LEVEL=debug         # Adjust log levels (info, debug, warn, error) for development/production
# NODE_ENV=development    # Set to "production" in a live environment
